
public class Sortierung <Veranstaltungen>{
  List<Veranstaltungen> sortierteListe = new List();

  public List<Veranstaltungen> ermittleSortiere()
  {
    int max=0;
    while(alteListe.isEmpty == false)
    {
      alteListe.toFirst;
      int max=0;
      while(alteListe.hasAccess=true){
          if(alteListe.gibAnzahlFreiePlaetze() > max)
          {
            max = alteListe.gibAnzahlFreiePlaetze;
          }
        alteListe.next();    
        }
      alteListe.toFirst;  
      while(alteListe.hasAccess=true){
        if(alteListe.gibAnzahlFreiePlaetze() = max){
          sortierteListe.append()=max;
          alteListe.remove();
          alteListe.next();  
          }
      }
      
    }                                                 
    
    
    
    
    
  } 
